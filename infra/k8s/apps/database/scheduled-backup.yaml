apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: cmc-db-nightly
spec:
  # Seconds field first — CNPG uses six-field cron. 02:30 UTC daily.
  schedule: '0 30 2 * * *'
  backupOwnerReference: self
  cluster:
    name: cmc-db
  immediate: true
