export default function Page() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col justify-center gap-4 px-6">
      <h1 className="text-4xl font-semibold tracking-tight">Hello World</h1>
      <p className="text-muted dark:text-neutral-400">
        Car Maintenance Companion — Phase 0 foundation. The production platform is being built
        before the product: this page exists to prove the pipeline reaches production, and nothing
        more.
      </p>
    </main>
  );
}
